**Diego Almeida — Backend Engineer**
7 years of experience in Python. I prefer `RLock` over simple `Lock` when there's
a chance a protected function calls another that also needs the same lock,
to avoid deadlock from re-entrancy. Input validation is always the
first line of any public function I write.